This file exists so Git preserves the `missing_source/` test fixture directory.

The fixture is intentionally an otherwise-empty repository root used to verify
behavior when no source tool directories are present, such as `.cursor/`,
`.claude/`, `.agents/skills/`, or `.codex/agents/`.
